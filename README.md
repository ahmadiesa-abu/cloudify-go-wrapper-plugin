cloudify-go-wrapper-plugin
========================

This plugin build a go lang as c library and call method from python

still in play mode


Pre-requisests:
* yum install -y gcc [ make sure that gcc is installed on your system ]